# Changesets

This folder stores release intent files for `dropin-template`.

## Create a changeset

```bash
npm run changeset
```

## Check pending release status

```bash
npm run changeset:status
```

## Apply version/changelog updates locally

```bash
npm run changeset:version
```
